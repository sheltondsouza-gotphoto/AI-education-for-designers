# Session 2 Scope (v2) — 2026-04-23

## Context

Session 1 ran on April 17. Topics not covered (confirmed via call transcript):
GitHub Desktop, `git push`, clone activity, APIs & REST, MCP Protocol.
Session 2 picks these up and adds npm/package managers.

## What was decided

**Session 2 confirmed topic list:**
1. GitHub Desktop — install walkthrough, visual diff, commit, push flow
2. `git push` — connecting local commits to GitHub remote
3. Clone the Repo — completing the skipped Session 1 hands-on activity (GitHub Desktop clone)
4. Package managers (npm) — what npm is, `package.json` anatomy, `npm run dev` explained
5. Mario game — everyone runs it locally at `localhost:5173` via `./setup.sh` + `npm run dev`
6. APIs & REST — GET/POST, JSON, browser DevTools Network tab, status codes
7. MCP Protocol — Host/Client/Server architecture + Figma connection explained

**Timing:** 7 blocks, 45 min. Clone activity (Block 3) is the must-complete — pause before moving on.

**GitHub CLI removed from session blocks** — too much setup risk for 45 min. Removed from plan and cheat sheet.

**Claude Code removed from session blocks** — moved to async homework. Link to docs only (same constraint as v1).

**Figma MCP:** Explain the Figma connection in Block 6 (previously "architecture only"). Use the filesystem MCP as the live demo. Figma MCP live setup deferred to a later session.

## What was rejected

- Keeping GitHub CLI as a dedicated block — adds install risk, not in the confirmed topic list.
- Claude Code live install during session — 45-min constraint, moved to homework.
- Live Figma MCP setup in Session 2 — deferred; filesystem MCP is sufficient as demo.
