For the second session, picking up from where Session 1 left off (mid-GitHub).

Topics:
Versioning — revision
GitHub & GitLab — revision (remote repos, branches, pull vs push)
GitHub Desktop — hands-on: branching, staging, history
GitHub CLI — installing gh CLI, running remote commands from terminal

Package Managers:
npm / bun — dependency graphs, node_modules, package.json scripts
What does `npm run dev` actually do?

APIs & REST — deeper dive
Request/response cycle, status codes, reading API docs
Browser DevTools Network tab demo

MCP Architecture:
Host / Client / Server model
How Claude connects to Figma, Slack, files via MCP

Claude Code:
Setup — link to docs only
Setting up your first MCP with Claude Code

Activity:
Run `npm run dev` on the mario game (now in sessions/mario-game/)
Explore package.json — understand what each script does
Set up Claude Code + one MCP server
